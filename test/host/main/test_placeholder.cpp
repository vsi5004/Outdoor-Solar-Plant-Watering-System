#include "unity.h"

// Phase 0 placeholder — delete once real tests cover everything.
static void test_pipeline_works(void)
{
    TEST_ASSERT_EQUAL(1, 1);
}

void run_placeholder_tests(void)
{
    RUN_TEST(test_pipeline_works);
}
